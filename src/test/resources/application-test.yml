server:
  port: 0
  servlet-path: /api/v1
spring:
  data:
    mongodb:
      host: localhost
      port: 0
      database: tgt
  jackson:
    joda-date-time-format: yyyy-MM-dd'T'HH:mm:ss.SSSZ
    serialization:
      write_dates_as_timestamps : false
# ===============================
# = ERROR HANDLING
# ===============================

logging:
  level:
    digital.fino: DEBUG
